<div class = "r-float">
  <%= link_to "Cancel", user_referees_path(@user) %>
</div>

<h1><%= @title %></h1> 
  <%= form_for([@user,@referee]) do |f| %>
    <%= render 'fields', :f => f %> 
    <div class="actions">
      <%= f.submit "Create" %>
    </div>
  <% end %>

