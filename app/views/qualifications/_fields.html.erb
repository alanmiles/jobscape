<%= render 'shared/error_messages', :object => f.object %>
<div class="field">
  <%= f.label :qualification %><br />
  <small>Academic or career-based.</small><br />
  <%= f.text_field :qualification, :maxlength => 50, :size => 40 %>
  <span id="char-count" style = "color:blue; padding-left: 5px"><%= @characters_left %></span>
</div>

<% content_for :javascript do %>

  <%= javascript_tag do %>

    $('#qualification_qualification').live('keyup keydown', function(e) {
    var maxLen = 50;
    var left = maxLen - $(this).val().length;
    $('#char-count').html(left);
    });
    
  <% end %>
<% end %>
