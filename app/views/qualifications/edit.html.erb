<div class = "r-float">
  <%= link_to "Cancel", user_qualifications_path(@qualification.user_id) %>
</div>

<h1><%= @title %></h1>
<br /><br />
<div class="shift_center">
  <%= form_for @qualification do |f| %>
    <%= render 'fields', :f => f %> 
    <div class="actions">
      <%= f.submit "Confirm changes" %>
    </div>
  <% end %>
</div>
