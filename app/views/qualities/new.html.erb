<%= link_to image_tag("pageback.png", :size => "25x25", :title => "Previous"), qualities_path %>


<div class="shift_center_50">
  <%= form_for(@quality) do |f| %>
    <%= render @quality, :f => f %>
    <div class="actions">
      <%= f.submit "Create Personal Attribute", :class => "create round" %>
    </div>
  <% end %>
</div>
