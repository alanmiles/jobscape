<div class="r-float">
  <%= link_to "Entire portrait", user_portrait_path(@strength.user) %>
</div>

<%= link_to image_tag("pageback.png", :title => "Strengths list"), user_strengths_path(@strength.user), :class => "magnify" %>

<h4 class="over"><%= @strength.user.name %></h4>

<div class="shift_center_50">
  <%= form_for @strength do |f| %>
    <%= render 'fields', :f => f %> 
    <div class="actions">
      <%= f.submit :class => "create round" %>
    </div>
  <% end %>
</div>
