<div class="r-float">
  <%= link_to "Entire portrait", user_portrait_path(@characteristic.user) %>
</div>

<%= link_to image_tag("pageback.png", :title => "Characteristics list"), user_characteristics_path(@characteristic.user), :class => "magnify" %>

<h4 class="over"><%= @characteristic.user.name %></h4>

<div class="shift_center_50">
  <%= form_for @characteristic do |f| %>
    <%= render 'fields', :f => f %> 
    <div class="actions">
      <%= f.submit :class => "create round" %>
    </div>
  <% end %>
</div>
