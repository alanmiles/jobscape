<div class = "r-float">
  <%= link_to "Cancel", user_previousjobs_path(@previousjob.user_id) %>
</div>

<h1><%= @title %></h1>
<br /><br />
<div class="shift_center">
  <%= form_for @previousjob do |f| %>
    <%= render 'fields', :f => f %> 
    <div class="actions">
      <%= f.submit "Confirm changes" %>
    </div>
  <% end %>
</div>
