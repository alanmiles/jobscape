<div class = "r-float">
  <%= link_to "Cancel", user_portrait_path(@user) %>
</div>

<h1><%= @title %></h1> 
<br /><br />
<div class="shift_center">
  <%= form_for([@user,@achievement]) do |f| %>
    <%= render 'fields', :f => f %> 
    <div class="actions">
      <%= f.submit "Create" %>
    </div>
  <% end %>
</div>
