<div class="r-float">
  <%= link_to "Entire portrait", user_portrait_path(@aim.user) %>
</div>

<%= link_to image_tag("pageback.png", :title => "Aims list"), user_aims_path(@aim.user), :class => "magnify" %>

<h4 class="over"><%= @aim.user.name %></h4>

<div class="shift_center_50">
  <%= form_for @aim do |f| %>
    <%= render 'fields', :f => f %> 
    <div class="actions">
      <%= f.submit :class => "create round" %>
    </div>
  <% end %>
</div>
